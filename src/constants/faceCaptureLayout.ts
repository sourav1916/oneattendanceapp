/** Oval face-alignment guide (shared across enroll + attendance capture). */
export const FACE_GUIDE_WIDTH = 240;
export const FACE_GUIDE_HEIGHT = 300;
export const FACE_GUIDE_RADIUS_X = FACE_GUIDE_WIDTH / 2;
export const FACE_GUIDE_RADIUS_Y = FACE_GUIDE_HEIGHT / 2;

/**
 * Shifts the guide upward so users place their face higher in frame
 * (front cameras sit above screen center).
 */
export const FACE_GUIDE_OFFSET_UP = -100;
